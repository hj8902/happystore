en:
  activerecord:
    errors:
      messages:
        record_invalid: "%{errors}"
    attributes:
      priceline/entity:
        id: id
        min_price: min price
        max_price: max price
  errors:
    format: "%{message}"